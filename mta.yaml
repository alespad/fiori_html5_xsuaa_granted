_schema-version: "3.2"
ID: testgrantedapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: testgrantedapp-destination-content
  type: com.sap.application.content
  requires:
  - name: testgrantedapp-destination-service
    parameters:
      content-target: true
  - name: testgrantedapp-repo-host
    parameters:
      service-key:
        name: testgrantedapp-repo-host-key
  - name: testgrantedapp-uaa
    parameters:
      service-key:
        name: testgrantedapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: testgrantedapp_html_repo_host
          ServiceInstanceName: testgrantedapp-html5-srv
          ServiceKeyName: testgrantedapp-repo-host-key
          sap.cloud.service: testgrantedapp
        - Authentication: OAuth2UserTokenExchange
          Name: testgrantedapp_uaa
          ServiceInstanceName: testgrantedapp-xsuaa-srv
          ServiceKeyName: testgrantedapp-uaa-key
          sap.cloud.service: testgrantedapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: testgrantedapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: testgrantedapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - testgrantedapp.zip
      name: testgrantedapp
      target-path: resources/
- name: testgrantedapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: testgrantedapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: testgrantedapp-destination-service
    service-plan: lite
- name: testgrantedapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: testgrantedapp-xsuaa-srv
    service-plan: application
- name: testgrantedapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: testgrantedapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
